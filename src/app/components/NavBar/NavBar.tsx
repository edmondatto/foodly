import * as React from 'react';
import './NavBar.css';

export const NavBar: React.SFC = () => {
    return(
        <nav>
            <a href="#" className={'nav-logo'}>Foodly</a>
        </nav>
    );
};
