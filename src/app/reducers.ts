const initialState = {
    error: '',
    fetching: false,
    recipe: null,
};

export function reducer(state = initialState, action: any) {
    switch (action.type) {
        default:
            return state;
    }
}